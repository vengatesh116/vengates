include<stdio.h>
int main()
{
int i,m,x,y,c,d;
printf("A robot have 2 type of commands\n");
scanf("%d",&m);
for(i=0;i<=x;i++)
{
if(x>=0 && x<=10000)
{
 printf("com1: forward%d",x);
}
}
for(i=0;i<=y;i++)
{
if(y==-180 && y==180)
{
printf("com2: X degree:%d",y);
}
}
return 0;
}